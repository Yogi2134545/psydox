# Bootstrap user definitions — imported ONCE per fresh DB, then ignored.
# SECURITY: password_hash values have been removed from this file.
# Existing production DB users are unaffected (migration skips existing rows).
# New deployments: set passwords via the admin panel or password-reset flow.
# This file is excluded from git via .gitignore.
#
# IMPORTANT: If this file exists in git history with real hashes,
# rotate ALL listed passwords immediately (BFG / git filter-branch needed
# to purge the history — that must be done manually outside this codebase).

ankit@popclub.co:
  name: Ankit
  role: manager
  password_hash: ""

bhargavi.kt@popclub.co:
  name: Bhargavi
  role: operator
  password_hash: ""

devesh@popclub.co:
  name: Devesh
  role: operator
  password_hash: ""

test@popclub.co:
  name: TestUser
  role: operator
  password_hash: ""

surya.pant@popclub.co:
  name: Surya
  role: admin
  password_hash: ""

# Owner is NOT listed here — the login bootstrap handles first-login creation.
# yogeshwar@popclub.co is auto-created with the submitted password on first login.
